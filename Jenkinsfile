pipeline {
    agent {
        label 'jenkin-service'
    }
    stages {
        stage('build') {
            steps {
                sh 'uname -a'
                sh 'echo main branch!'
            }
        }
    }
}
